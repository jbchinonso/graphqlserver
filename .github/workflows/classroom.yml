    name: GitHub Classroom Workflow

    on: [push]

    jobs:
      build:
        name: Autograding
        runs-on: ubuntu-latest
        env:
          secret: randomsecretfortoken

        steps:
          - name: action time
            run: echo '::set-env name=secret::randomsecretfortoken'
          - name: action echo
            run: echo 'The selected color is' $secret
          - uses: actions/checkout@v2
          - uses: education/autograding@v1

           
