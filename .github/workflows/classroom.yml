    name: GitHub Classroom Workflow

    on: [push]

    jobs:
      build:
        name: Autograding
        runs-on: ubuntu-latest
        env:
          secret: randomsecretfortoken

        steps:
          - uses: actions/checkout@v2
          - name: create env file
            run: |
                touch .env
                echo "secret='randomsecretfortoken'" >> .env
          - name: show env file
            run: ls
            env:
              secret: randomsecretfortoken
          - uses: education/autograding@v1

           
